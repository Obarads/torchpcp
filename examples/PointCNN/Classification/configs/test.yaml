# General
general:
  ## seed value
  seed: 71
  ## reproducibility
  reproducibility: False
  ## use cuda
  device: cuda
  batch_size: 32

# dataset settings
dataset:
  name: modelnet40
  root: ???

loader:
  nworkers: 32

# Network settings
model:
  ## path to a trained model
  resume: ???

