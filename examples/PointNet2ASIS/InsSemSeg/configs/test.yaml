# General
general:
  ## seed value
  seed: 71
  ## reproducibility
  reproducibility: False
  ## use cuda
  device: cuda
  ## get mean_ins_size.txt
  sizetxt: mean_ins_size.txt

# dataset settings
dataset:
  name: s3dis
  mode: scene
  root: ???
  

# Network settings
model:
  ## path to a trained model
  resume: ???

